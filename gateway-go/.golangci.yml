version: "2"
linters:
  default: standard
  enable: [revive, gocyclo, gosec, bodyclose, contextcheck, errorlint, goconst]
  settings:
    gocyclo:
      min-complexity: 10
formatters:
  enable: [gofmt, goimports]
