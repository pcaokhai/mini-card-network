/**
 * This file is AUTO GENERATED by [msw-auto-mock](https://github.com/zoubingwu/msw-auto-mock)
 * Feel free to commit/edit it as you need.
 */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { HttpResponse, http } from "msw";
import { faker } from "@faker-js/faker";

faker.seed(1);

const baseURL = "/api";
const MAX_ARRAY_LENGTH = 20;
const MAX_STRING_LENGTH = 42;

// Map to store counters for each API endpoint
const apiCounters = new Map<string, number>();

const next = (apiKey: string) => {
  let currentCount = apiCounters.get(apiKey) ?? 0;
  if (currentCount === Number.MAX_SAFE_INTEGER - 1) {
    currentCount = 0;
  }
  apiCounters.set(apiKey, currentCount + 1);
  return currentCount;
};

export const handlers = [
  http.get(`${baseURL}/v1/lab/messages/samples`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListSampleMessages200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/lab/messages/samples`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/v1/keys/acquirer/rotations`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getStartKeyRotation202Response(), { status: 202 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /v1/keys/acquirer/rotations`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/v1/lab/messages/decode`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getDecodeMessage200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /v1/lab/messages/decode`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/v1/lab/messages/encode`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getEncodeMessage200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /v1/lab/messages/encode`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/chaos/scenarios`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListChaosScenarios200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/chaos/scenarios`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/keys/acquirer`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListAcquirerKeys200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/keys/acquirer`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/keys/issuer`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListIssuerKeys200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/keys/issuer`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/metrics/overview`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetOverview200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/metrics/overview`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/network/events`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListNetworkEvents200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/network/events`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/network/links`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListLinks200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/network/links`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/network/saf`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[await getGetSaf200Response(), { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /v1/network/saf`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/network/switch`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetSwitchStatus200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/network/switch`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/v1/chaos/runs`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getStartChaosRun202Response(), { status: 202 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /v1/chaos/runs`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/v1/transactions/balance-inquiries`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getCreateBalanceInquiry201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/transactions/balance-inquiries`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/transactions/pre-authorizations`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getCreatePreAuthorization201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/transactions/pre-authorizations`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(`${baseURL}/v1/transactions/purchases`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getCreatePurchase201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /v1/transactions/purchases`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/v1/transactions/refunds`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getCreateRefund201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /v1/transactions/refunds`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/cards`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListCards200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/cards`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/terminals`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListTerminals200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/terminals`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/transactions`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getListTransactions200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/transactions`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/v1/keys/acquirer/rotations/:rotationId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getGetKeyRotation200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /v1/keys/acquirer/rotations/:rotationId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.get(
    `${baseURL}/v1/settlement/days/:businessDate/breaks`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getListBreaks200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /v1/settlement/days/:businessDate/breaks`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(`${baseURL}/v1/network/links/:linkId/echo`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[await getEchoLink200Response(), { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /v1/network/links/:linkId/echo`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/v1/network/links/:linkId/sign-off`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getSignOffLink200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/network/links/:linkId/sign-off`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/network/links/:linkId/sign-on`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getSignOnLink200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/network/links/:linkId/sign-on`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/settlement/breaks/:breakId/resolutions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getResolveBreak200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/settlement/breaks/:breakId/resolutions`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/settlement/days/:businessDate/clearing-files`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getGenerateClearingFile201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/settlement/days/:businessDate/clearing-files`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/settlement/days/:businessDate/cutover`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getTriggerCutover202Response(), { status: 202 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/settlement/days/:businessDate/cutover`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/settlement/days/:businessDate/reconciliations`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getRunReconciliation202Response(), { status: 202 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/settlement/days/:businessDate/reconciliations`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.delete(`${baseURL}/v1/cards/:cardRef/blocks`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("delete")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUnblockCard200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`delete /v1/cards/:cardRef/blocks`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/cards/:cardRef/ledger`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetCardLedger200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/cards/:cardRef/ledger`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/chaos/runs/:runId`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetChaosRun200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/chaos/runs/:runId`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/v1/settlement/days/:businessDate`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getGetSettlementDay200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /v1/settlement/days/:businessDate`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.get(`${baseURL}/v1/transactions/:rrn/journey`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetTransactionJourney200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /v1/transactions/:rrn/journey`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/v1/cards/:cardRef/blocks`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBlockCard200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /v1/cards/:cardRef/blocks`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/v1/transactions/:rrn/cancellations`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getCancelTransaction202Response(), { status: 202 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/transactions/:rrn/cancellations`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/v1/transactions/:rrn/completions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getCreateCompletion201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /v1/transactions/:rrn/completions`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.put(`${baseURL}/v1/cards/:cardRef/limits`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("put")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUpdateCardLimits200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`put /v1/cards/:cardRef/limits`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.put(`${baseURL}/v1/chaos/scenarios/:scenarioId`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("put")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getSetChaosScenario200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`put /v1/chaos/scenarios/:scenarioId`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/cards/:cardRef`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[await getGetCard200Response(), { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /v1/cards/:cardRef`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/v1/transactions/:rrn`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetTransaction200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /v1/transactions/:rrn`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
];

export function getCreatePurchase201Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getCreatePreAuthorization201Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getCreateCompletion201Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getCreateRefund201Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getCreateBalanceInquiry201Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getCancelTransaction202Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getListTransactions200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        rrn: faker.lorem.words(),
        stan: faker.lorem.words(),
        type: faker.helpers.arrayElement([
          "PURCHASE",
          "PREAUTH",
          "COMPLETION",
          "REFUND",
          "BALANCE",
          "REVERSAL",
        ]),
        status: faker.helpers.arrayElement([
          "CREATED",
          "SENT",
          "APPROVED",
          "DECLINED",
          "TIMED_OUT",
          "REVERSAL_PENDING",
          "REVERSED",
          "FAILED",
        ]),
        responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
        responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
        amount: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        maskedPan: faker.helpers.fromRegExp(
          new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
        ),
        terminalId: faker.string.uuid(),
        merchantName: faker.person.fullName(),
        latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    nextCursor: faker.helpers.arrayElement([faker.lorem.words(), null]),
  };
}

export function getGetTransaction200Response() {
  return {
    rrn: faker.lorem.words(),
    stan: faker.lorem.words(),
    type: faker.helpers.arrayElement([
      "PURCHASE",
      "PREAUTH",
      "COMPLETION",
      "REFUND",
      "BALANCE",
      "REVERSAL",
    ]),
    status: faker.helpers.arrayElement([
      "CREATED",
      "SENT",
      "APPROVED",
      "DECLINED",
      "TIMED_OUT",
      "REVERSAL_PENDING",
      "REVERSED",
      "FAILED",
    ]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
    amount: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    maskedPan: faker.helpers.fromRegExp(
      new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
    ),
    terminalId: faker.string.uuid(),
    merchantName: faker.person.fullName(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    createdAt: faker.date.anytime().toISOString(),
    authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
    approvedAmount: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    balance: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    businessDate: faker.date.past().toISOString().substring(0, 10),
    originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
    traceId: faker.string.uuid(),
  };
}

export function getGetTransactionJourney200Response() {
  return {
    transaction: {
      rrn: faker.lorem.words(),
      stan: faker.lorem.words(),
      type: faker.helpers.arrayElement([
        "PURCHASE",
        "PREAUTH",
        "COMPLETION",
        "REFUND",
        "BALANCE",
        "REVERSAL",
      ]),
      status: faker.helpers.arrayElement([
        "CREATED",
        "SENT",
        "APPROVED",
        "DECLINED",
        "TIMED_OUT",
        "REVERSAL_PENDING",
        "REVERSED",
        "FAILED",
      ]),
      responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
      responseLabel: faker.helpers.arrayElement([faker.lorem.words(), null]),
      amount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      maskedPan: faker.helpers.fromRegExp(
        new RegExp("^[0-9]{6}\\*{2,9}[0-9]{4}$"),
      ),
      terminalId: faker.string.uuid(),
      merchantName: faker.person.fullName(),
      latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
      createdAt: faker.date.anytime().toISOString(),
      authCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
      approvedAmount: faker.helpers.arrayElement([
        {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        null,
      ]),
      balance: faker.helpers.arrayElement([
        {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        null,
      ]),
      businessDate: faker.date.past().toISOString().substring(0, 10),
      originalRrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
      traceId: faker.string.uuid(),
    },
    steps: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        seq: faker.number.int(),
        actor: faker.helpers.arrayElement([
          "POS",
          "ACQUIRER",
          "ISSUER",
          "SAF",
          "NETWORK",
          "SWITCH",
        ]),
        offsetMs: faker.number.int(),
        title: faker.lorem.words(),
        easyText: faker.lorem.words(),
        technicalText: faker.lorem.words(),
        kind: faker.helpers.arrayElement([
          "OK",
          "WARN",
          "BAD",
          "REVERSAL",
          "INFO",
        ]),
        code: faker.helpers.arrayElement([
          "POS_REQUEST",
          "REQUEST_SENT",
          "LOCAL_DECLINE",
          "ISSUER_APPROVED",
          "ISSUER_DECLINED",
          "NO_RESPONSE",
          "REVERSAL_QUEUED",
          "POS_RESULT",
          "REVERSAL_SENT",
          "REVERSAL_CONFIRMED",
          "LATE_RESPONSE",
        ]),
        message: faker.helpers.arrayElement([
          {
            mti: faker.lorem.words(),
            fields: (() => {
              const arrayMin = 1;
              const arrayMax = MAX_ARRAY_LENGTH;
              const safeMin = Math.min(arrayMin, arrayMax);
              return [
                ...new Array(
                  faker.number.int({ min: safeMin, max: arrayMax }),
                ).keys(),
              ].map((_) => ({
                de: faker.lorem.words(),
                easyName: faker.person.fullName(),
                technicalName: faker.person.fullName(),
                format: faker.lorem.words(),
                value: faker.lorem.words(),
                raw: faker.lorem.words(),
              }));
            })(),
          },
          null,
        ]),
      }));
    })(),
    money: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        label: faker.lorem.words(),
        delta: faker.number.int(),
        balanceAfter: faker.helpers.arrayElement([faker.number.int(), null]),
        atStep: faker.number.int(),
      }));
    })(),
  };
}

export function getListTerminals200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      terminalId: faker.string.uuid(),
      merchantId: faker.string.uuid(),
      merchantName: faker.person.fullName(),
      mcc: faker.lorem.words(),
    }));
  })();
}

export function getDecodeMessage200Response() {
  return {
    mti: faker.lorem.words(),
    primaryBitmap: faker.helpers.fromRegExp(new RegExp("^[0-9A-F]{16}$")),
    secondaryBitmap: faker.helpers.arrayElement([
      faker.helpers.fromRegExp(new RegExp("^[0-9A-F]{16}$")),
      null,
    ]),
    segments: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        key: faker.lorem.words(),
        text: faker.lorem.words(),
      }));
    })(),
    fields: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        de: faker.lorem.words(),
        easyName: faker.person.fullName(),
        technicalName: faker.person.fullName(),
        format: faker.lorem.words(),
        value: faker.lorem.words(),
        raw: faker.lorem.words(),
      }));
    })(),
  };
}

export function getEncodeMessage200Response() {
  return {
    mti: faker.lorem.words(),
    primaryBitmap: faker.helpers.fromRegExp(new RegExp("^[0-9A-F]{16}$")),
    secondaryBitmap: faker.helpers.arrayElement([
      faker.helpers.fromRegExp(new RegExp("^[0-9A-F]{16}$")),
      null,
    ]),
    segments: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        key: faker.lorem.words(),
        text: faker.lorem.words(),
      }));
    })(),
    fields: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        de: faker.lorem.words(),
        easyName: faker.person.fullName(),
        technicalName: faker.person.fullName(),
        format: faker.lorem.words(),
        value: faker.lorem.words(),
        raw: faker.lorem.words(),
      }));
    })(),
  };
}

export function getListSampleMessages200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      mti: faker.lorem.words(),
      label: faker.lorem.words(),
      raw: faker.lorem.words(),
    }));
  })();
}

export function getListLinks200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      linkId: faker.string.uuid(),
      from: faker.lorem.words(),
      to: faker.lorem.words(),
      status: faker.helpers.arrayElement([
        "DISCONNECTED",
        "CONNECTED",
        "SIGNED_ON",
        "DOWN",
      ]),
      lastEchoAt: faker.helpers.arrayElement([
        faker.date.anytime().toISOString(),
        null,
      ]),
      lastEchoOk: faker.helpers.arrayElement([faker.datatype.boolean(), null]),
      p99LatencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
      inFlight: faker.number.int(),
    }));
  })();
}

export function getEchoLink200Response() {
  return {
    ok: faker.datatype.boolean(),
    latencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    responseCode: faker.helpers.arrayElement([faker.lorem.words(), null]),
  };
}

export function getSignOnLink200Response() {
  return {
    linkId: faker.string.uuid(),
    from: faker.lorem.words(),
    to: faker.lorem.words(),
    status: faker.helpers.arrayElement([
      "DISCONNECTED",
      "CONNECTED",
      "SIGNED_ON",
      "DOWN",
    ]),
    lastEchoAt: faker.helpers.arrayElement([
      faker.date.anytime().toISOString(),
      null,
    ]),
    lastEchoOk: faker.helpers.arrayElement([faker.datatype.boolean(), null]),
    p99LatencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    inFlight: faker.number.int(),
  };
}

export function getSignOffLink200Response() {
  return {
    linkId: faker.string.uuid(),
    from: faker.lorem.words(),
    to: faker.lorem.words(),
    status: faker.helpers.arrayElement([
      "DISCONNECTED",
      "CONNECTED",
      "SIGNED_ON",
      "DOWN",
    ]),
    lastEchoAt: faker.helpers.arrayElement([
      faker.date.anytime().toISOString(),
      null,
    ]),
    lastEchoOk: faker.helpers.arrayElement([faker.datatype.boolean(), null]),
    p99LatencyMs: faker.helpers.arrayElement([faker.number.int(), null]),
    inFlight: faker.number.int(),
  };
}

export function getGetSaf200Response() {
  return {
    depth: faker.number.int(),
    deadCount: faker.number.int(),
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        mti: faker.helpers.arrayElement(["0120", "0220", "0420"]),
        rrn: faker.lorem.words(),
        amount: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        attempts: faker.number.int(),
        status: faker.helpers.arrayElement([
          "PENDING",
          "IN_FLIGHT",
          "ACKED",
          "DEAD",
        ]),
        nextRetryAt: faker.date.anytime().toISOString(),
        lastError: faker.helpers.arrayElement([faker.lorem.words(), null]),
      }));
    })(),
  };
}

export function getListNetworkEvents200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        occurredAt: faker.date.anytime().toISOString(),
        severity: faker.helpers.arrayElement(["INFO", "OK", "WARN", "ERROR"]),
        easyText: faker.lorem.words(),
        technicalText: faker.lorem.words(),
      }));
    })(),
    nextCursor: faker.helpers.arrayElement([faker.lorem.words(), null]),
  };
}

export function getGetSwitchStatus200Response() {
  return {
    circuit: faker.helpers.arrayElement(["CLOSED", "OPEN", "HALF_OPEN"]),
    stipActive: faker.datatype.boolean(),
    stipLimit: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    stipApprovedCount: faker.number.int(),
  };
}

export function getListChaosScenarios200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.helpers.arrayElement([
        "SLOW_NETWORK",
        "CONNECTION_CUT",
        "DROP_RESPONSE",
        "DUPLICATE_REQUEST",
        "ISSUER_DOWN",
        "LATE_RESPONSE",
      ]),
      enabled: faker.datatype.boolean(),
      easyText: faker.lorem.words(),
      technicalText: faker.lorem.words(),
    }));
  })();
}

export function getSetChaosScenario200Response() {
  return {
    id: faker.helpers.arrayElement([
      "SLOW_NETWORK",
      "CONNECTION_CUT",
      "DROP_RESPONSE",
      "DUPLICATE_REQUEST",
      "ISSUER_DOWN",
      "LATE_RESPONSE",
    ]),
    enabled: faker.datatype.boolean(),
    easyText: faker.lorem.words(),
    technicalText: faker.lorem.words(),
  };
}

export function getStartChaosRun202Response() {
  return {
    runId: faker.string.uuid(),
    status: faker.helpers.arrayElement([
      "RUNNING",
      "VERIFYING",
      "PASSED",
      "FAILED",
    ]),
    requested: faker.number.int(),
    completed: faker.number.int(),
    approved: faker.number.int(),
    declined: faker.number.int(),
    reversed: faker.number.int(),
    openingBalanceTotal: faker.number.int(),
    closingBalanceTotal: faker.number.int(),
    ledgerDiscrepancy: faker.number.int(),
  };
}

export function getGetChaosRun200Response() {
  return {
    runId: faker.string.uuid(),
    status: faker.helpers.arrayElement([
      "RUNNING",
      "VERIFYING",
      "PASSED",
      "FAILED",
    ]),
    requested: faker.number.int(),
    completed: faker.number.int(),
    approved: faker.number.int(),
    declined: faker.number.int(),
    reversed: faker.number.int(),
    openingBalanceTotal: faker.number.int(),
    closingBalanceTotal: faker.number.int(),
    ledgerDiscrepancy: faker.number.int(),
  };
}

export function getGetOverview200Response() {
  return {
    transactionsToday: faker.number.int(),
    transactionsDeltaPct: faker.number.int(),
    approvalRate: faker.number.int(),
    p99LatencyMs: faker.number.int(),
    p50LatencyMs: faker.number.int(),
    ledgerMatches: faker.datatype.boolean(),
    throughput: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        at: faker.date.anytime().toISOString(),
        tps: faker.number.int(),
      }));
    })(),
    declineReasons: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        responseCode: faker.lorem.words(),
        label: faker.lorem.words(),
        share: faker.number.int(),
      }));
    })(),
  };
}

export function getListCards200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      cardRef: faker.lorem.words(),
      maskedPan: faker.lorem.words(),
      holderName: faker.person.fullName(),
      status: faker.helpers.arrayElement([
        "ACTIVE",
        "BLOCKED",
        "LOST",
        "STOLEN",
        "EXPIRED",
        "PIN_BLOCKED",
      ]),
      expiry: faker.helpers.fromRegExp(new RegExp("^[0-9]{2}/[0-9]{2}$")),
    }));
  })();
}

export function getGetCard200Response() {
  return {
    cardRef: faker.lorem.words(),
    maskedPan: faker.lorem.words(),
    holderName: faker.person.fullName(),
    status: faker.helpers.arrayElement([
      "ACTIVE",
      "BLOCKED",
      "LOST",
      "STOLEN",
      "EXPIRED",
      "PIN_BLOCKED",
    ]),
    expiry: faker.helpers.fromRegExp(new RegExp("^[0-9]{2}/[0-9]{2}$")),
    ledgerBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    availableBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    holds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        holdId: faker.string.uuid(),
        merchantName: faker.person.fullName(),
        amount: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        expiresAt: faker.date.anytime().toISOString(),
        status: faker.helpers.arrayElement([
          "ACTIVE",
          "COMPLETED",
          "RELEASED",
          "EXPIRED",
        ]),
      }));
    })(),
    limits: {
      dailyAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      perTransactionAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      dailyCount: faker.helpers.arrayElement([faker.number.int(), null]),
    },
    usedToday: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
  };
}

export function getBlockCard200Response() {
  return {
    cardRef: faker.lorem.words(),
    maskedPan: faker.lorem.words(),
    holderName: faker.person.fullName(),
    status: faker.helpers.arrayElement([
      "ACTIVE",
      "BLOCKED",
      "LOST",
      "STOLEN",
      "EXPIRED",
      "PIN_BLOCKED",
    ]),
    expiry: faker.helpers.fromRegExp(new RegExp("^[0-9]{2}/[0-9]{2}$")),
    ledgerBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    availableBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    holds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        holdId: faker.string.uuid(),
        merchantName: faker.person.fullName(),
        amount: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        expiresAt: faker.date.anytime().toISOString(),
        status: faker.helpers.arrayElement([
          "ACTIVE",
          "COMPLETED",
          "RELEASED",
          "EXPIRED",
        ]),
      }));
    })(),
    limits: {
      dailyAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      perTransactionAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      dailyCount: faker.helpers.arrayElement([faker.number.int(), null]),
    },
    usedToday: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
  };
}

export function getUnblockCard200Response() {
  return {
    cardRef: faker.lorem.words(),
    maskedPan: faker.lorem.words(),
    holderName: faker.person.fullName(),
    status: faker.helpers.arrayElement([
      "ACTIVE",
      "BLOCKED",
      "LOST",
      "STOLEN",
      "EXPIRED",
      "PIN_BLOCKED",
    ]),
    expiry: faker.helpers.fromRegExp(new RegExp("^[0-9]{2}/[0-9]{2}$")),
    ledgerBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    availableBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    holds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        holdId: faker.string.uuid(),
        merchantName: faker.person.fullName(),
        amount: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        expiresAt: faker.date.anytime().toISOString(),
        status: faker.helpers.arrayElement([
          "ACTIVE",
          "COMPLETED",
          "RELEASED",
          "EXPIRED",
        ]),
      }));
    })(),
    limits: {
      dailyAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      perTransactionAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      dailyCount: faker.helpers.arrayElement([faker.number.int(), null]),
    },
    usedToday: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
  };
}

export function getUpdateCardLimits200Response() {
  return {
    cardRef: faker.lorem.words(),
    maskedPan: faker.lorem.words(),
    holderName: faker.person.fullName(),
    status: faker.helpers.arrayElement([
      "ACTIVE",
      "BLOCKED",
      "LOST",
      "STOLEN",
      "EXPIRED",
      "PIN_BLOCKED",
    ]),
    expiry: faker.helpers.fromRegExp(new RegExp("^[0-9]{2}/[0-9]{2}$")),
    ledgerBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    availableBalance: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    holds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        holdId: faker.string.uuid(),
        merchantName: faker.person.fullName(),
        amount: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        expiresAt: faker.date.anytime().toISOString(),
        status: faker.helpers.arrayElement([
          "ACTIVE",
          "COMPLETED",
          "RELEASED",
          "EXPIRED",
        ]),
      }));
    })(),
    limits: {
      dailyAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      perTransactionAmount: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      dailyCount: faker.helpers.arrayElement([faker.number.int(), null]),
    },
    usedToday: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
  };
}

export function getGetCardLedger200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        journalId: faker.string.uuid(),
        occurredAt: faker.date.anytime().toISOString(),
        description: faker.lorem.words(),
        entryType: faker.helpers.arrayElement([
          "PURCHASE",
          "CASH",
          "REFUND",
          "REVERSAL",
          "COMPLETION",
          "FEE",
          "ADJUSTMENT",
        ]),
        rrn: faker.helpers.arrayElement([faker.lorem.words(), null]),
        postings: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax }),
            ).keys(),
          ].map((_) => ({
            account: faker.lorem.words(),
            direction: faker.helpers.arrayElement(["DEBIT", "CREDIT"]),
            amount: {
              amount: faker.number.int(),
              currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
            },
          }));
        })(),
      }));
    })(),
    nextCursor: faker.helpers.arrayElement([faker.lorem.words(), null]),
  };
}

export function getListIssuerKeys200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      keyType: faker.helpers.arrayElement([
        "ZMK",
        "ZPK",
        "ZAK",
        "TPK",
        "TAK",
        "CVK",
        "PVK",
      ]),
      counterparty: faker.helpers.arrayElement([faker.lorem.words(), null]),
      kcv: faker.helpers.fromRegExp(new RegExp("^[0-9A-F]{6}$")),
      status: faker.helpers.arrayElement(["PENDING", "ACTIVE", "RETIRED"]),
      activatedAt: faker.helpers.arrayElement([
        faker.date.anytime().toISOString(),
        null,
      ]),
      daysRemaining: faker.number.int(),
      lifetimeDays: faker.number.int(),
    }));
  })();
}

export function getListAcquirerKeys200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      keyType: faker.helpers.arrayElement([
        "ZMK",
        "ZPK",
        "ZAK",
        "TPK",
        "TAK",
        "CVK",
        "PVK",
      ]),
      counterparty: faker.helpers.arrayElement([faker.lorem.words(), null]),
      kcv: faker.helpers.fromRegExp(new RegExp("^[0-9A-F]{6}$")),
      status: faker.helpers.arrayElement(["PENDING", "ACTIVE", "RETIRED"]),
      activatedAt: faker.helpers.arrayElement([
        faker.date.anytime().toISOString(),
        null,
      ]),
      daysRemaining: faker.number.int(),
      lifetimeDays: faker.number.int(),
    }));
  })();
}

export function getStartKeyRotation202Response() {
  return {
    rotationId: faker.string.uuid(),
    keyType: faker.lorem.words(),
    status: faker.helpers.arrayElement(["RUNNING", "COMPLETED", "FAILED"]),
    newKcv: faker.helpers.arrayElement([faker.lorem.words(), null]),
    steps: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.helpers.arrayElement([
          "GENERATE",
          "SEND_0800_161",
          "PARTNER_CONFIRM",
          "ACTIVATE",
        ]),
        status: faker.helpers.arrayElement(["PENDING", "DONE", "FAILED"]),
        completedAt: faker.helpers.arrayElement([
          faker.date.anytime().toISOString(),
          null,
        ]),
      }));
    })(),
  };
}

export function getGetKeyRotation200Response() {
  return {
    rotationId: faker.string.uuid(),
    keyType: faker.lorem.words(),
    status: faker.helpers.arrayElement(["RUNNING", "COMPLETED", "FAILED"]),
    newKcv: faker.helpers.arrayElement([faker.lorem.words(), null]),
    steps: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.helpers.arrayElement([
          "GENERATE",
          "SEND_0800_161",
          "PARTNER_CONFIRM",
          "ACTIVATE",
        ]),
        status: faker.helpers.arrayElement(["PENDING", "DONE", "FAILED"]),
        completedAt: faker.helpers.arrayElement([
          faker.date.anytime().toISOString(),
          null,
        ]),
      }));
    })(),
  };
}

export function getGetSettlementDay200Response() {
  return {
    businessDate: faker.date.past().toISOString().substring(0, 10),
    stage: faker.helpers.arrayElement([
      "OPEN",
      "CUTOVER_DONE",
      "TOTALS_EXCHANGED",
      "RECONCILED",
      "FILE_GENERATED",
    ]),
    totals: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        metric: faker.helpers.arrayElement([
          "DEBITS_COUNT",
          "DEBITS_AMOUNT",
          "DEBIT_REVERSALS_COUNT",
          "DEBIT_REVERSALS_AMOUNT",
          "NET_AMOUNT",
        ]),
        isoField: faker.lorem.words(),
        acquirer: faker.number.int(),
        issuer: faker.number.int(),
        matches: faker.datatype.boolean(),
      }));
    })(),
    matchedCount: faker.helpers.arrayElement([faker.number.int(), null]),
    totalCount: faker.helpers.arrayElement([faker.number.int(), null]),
    openBreaks: faker.number.int(),
    netPosition: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    clearingFile: faker.helpers.arrayElement([
      {
        fileName: faker.person.fullName(),
        recordCount: faker.number.int(),
        total: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        sha256: faker.helpers.fromRegExp(new RegExp("^[0-9a-f]{64}$")),
        status: faker.helpers.arrayElement([
          "GENERATED",
          "SENT",
          "ACKNOWLEDGED",
          "REJECTED",
        ]),
      },
      null,
    ]),
  };
}

export function getTriggerCutover202Response() {
  return {
    businessDate: faker.date.past().toISOString().substring(0, 10),
    stage: faker.helpers.arrayElement([
      "OPEN",
      "CUTOVER_DONE",
      "TOTALS_EXCHANGED",
      "RECONCILED",
      "FILE_GENERATED",
    ]),
    totals: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        metric: faker.helpers.arrayElement([
          "DEBITS_COUNT",
          "DEBITS_AMOUNT",
          "DEBIT_REVERSALS_COUNT",
          "DEBIT_REVERSALS_AMOUNT",
          "NET_AMOUNT",
        ]),
        isoField: faker.lorem.words(),
        acquirer: faker.number.int(),
        issuer: faker.number.int(),
        matches: faker.datatype.boolean(),
      }));
    })(),
    matchedCount: faker.helpers.arrayElement([faker.number.int(), null]),
    totalCount: faker.helpers.arrayElement([faker.number.int(), null]),
    openBreaks: faker.number.int(),
    netPosition: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    clearingFile: faker.helpers.arrayElement([
      {
        fileName: faker.person.fullName(),
        recordCount: faker.number.int(),
        total: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        sha256: faker.helpers.fromRegExp(new RegExp("^[0-9a-f]{64}$")),
        status: faker.helpers.arrayElement([
          "GENERATED",
          "SENT",
          "ACKNOWLEDGED",
          "REJECTED",
        ]),
      },
      null,
    ]),
  };
}

export function getRunReconciliation202Response() {
  return {
    businessDate: faker.date.past().toISOString().substring(0, 10),
    stage: faker.helpers.arrayElement([
      "OPEN",
      "CUTOVER_DONE",
      "TOTALS_EXCHANGED",
      "RECONCILED",
      "FILE_GENERATED",
    ]),
    totals: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        metric: faker.helpers.arrayElement([
          "DEBITS_COUNT",
          "DEBITS_AMOUNT",
          "DEBIT_REVERSALS_COUNT",
          "DEBIT_REVERSALS_AMOUNT",
          "NET_AMOUNT",
        ]),
        isoField: faker.lorem.words(),
        acquirer: faker.number.int(),
        issuer: faker.number.int(),
        matches: faker.datatype.boolean(),
      }));
    })(),
    matchedCount: faker.helpers.arrayElement([faker.number.int(), null]),
    totalCount: faker.helpers.arrayElement([faker.number.int(), null]),
    openBreaks: faker.number.int(),
    netPosition: faker.helpers.arrayElement([
      {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      null,
    ]),
    clearingFile: faker.helpers.arrayElement([
      {
        fileName: faker.person.fullName(),
        recordCount: faker.number.int(),
        total: {
          amount: faker.number.int(),
          currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
        },
        sha256: faker.helpers.fromRegExp(new RegExp("^[0-9a-f]{64}$")),
        status: faker.helpers.arrayElement([
          "GENERATED",
          "SENT",
          "ACKNOWLEDGED",
          "REJECTED",
        ]),
      },
      null,
    ]),
  };
}

export function getListBreaks200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      breakId: faker.string.uuid(),
      breakType: faker.helpers.arrayElement([
        "MISSING_AT_ISSUER",
        "MISSING_AT_ACQUIRER",
        "AMOUNT_MISMATCH",
        "STATUS_MISMATCH",
        "DUPLICATE",
      ]),
      rrn: faker.lorem.words(),
      amountDiff: {
        amount: faker.number.int(),
        currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
      },
      easyText: faker.lorem.words(),
      technicalText: faker.lorem.words(),
      resolution: faker.helpers.arrayElement([
        "OPEN",
        "AUTO_RESOLVED",
        "MANUAL_ADJUSTED",
        "WRITTEN_OFF",
      ]),
      resolvedBy: faker.helpers.arrayElement([faker.lorem.words(), null]),
    }));
  })();
}

export function getResolveBreak200Response() {
  return {
    breakId: faker.string.uuid(),
    breakType: faker.helpers.arrayElement([
      "MISSING_AT_ISSUER",
      "MISSING_AT_ACQUIRER",
      "AMOUNT_MISMATCH",
      "STATUS_MISMATCH",
      "DUPLICATE",
    ]),
    rrn: faker.lorem.words(),
    amountDiff: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    easyText: faker.lorem.words(),
    technicalText: faker.lorem.words(),
    resolution: faker.helpers.arrayElement([
      "OPEN",
      "AUTO_RESOLVED",
      "MANUAL_ADJUSTED",
      "WRITTEN_OFF",
    ]),
    resolvedBy: faker.helpers.arrayElement([faker.lorem.words(), null]),
  };
}

export function getGenerateClearingFile201Response() {
  return {
    fileName: faker.person.fullName(),
    recordCount: faker.number.int(),
    total: {
      amount: faker.number.int(),
      currency: faker.helpers.fromRegExp(new RegExp("^[0-9]{3}$")),
    },
    sha256: faker.helpers.fromRegExp(new RegExp("^[0-9a-f]{64}$")),
    status: faker.helpers.arrayElement([
      "GENERATED",
      "SENT",
      "ACKNOWLEDGED",
      "REJECTED",
    ]),
  };
}
